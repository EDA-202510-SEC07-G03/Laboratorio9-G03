from DataStructures.Tree import rbt_node as rbt
def new_tree():
    root = None
    return None

def is_red(node_rbt):
    bol = rbt.is_red(node_rbt)
    if bol:
        return True
    else:
        return False